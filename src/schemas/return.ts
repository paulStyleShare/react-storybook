export interface ReturnItemSchema {
  orderId: string;
  returnItemId: string;
  status: string;
  title: string;
  createdAt: string;
  goodsId: number;
  pictureId: string;
  name: string;
  options: string[];
  detailPageUrl: string;
}
